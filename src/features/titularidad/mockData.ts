// =============================================================
// Portal Condominio · Titularidad — datos mock
// SIN datos hardcodeados: la bandeja arranca vacía. Las solicitudes reales
// entrarán por el flujo del solicitante + verificación registral. // SWAP POINT.
// El desarrollo es "Margot" explícito (nunca id_proyecto=1 por defecto).
// =============================================================
import type { SolicitudTitularidad } from "./types";

export const MOCK_SOLICITUDES: SolicitudTitularidad[] = [];
