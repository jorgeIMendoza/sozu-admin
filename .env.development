VITE_ENVIRONMENT="preview"
VITE_N8N_WEBHOOK_BASE_URL="https://n8n-dev.sozu.com/webhook"
# Supabase: credentials inherited from .env (real instance)
# Do NOT add VITE_SUPABASE_* here — config.ts gives .env.development priority
# and the demo values below would override the real Supabase credentials.
